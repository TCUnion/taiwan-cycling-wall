# 相揪約騎公布欄 — SEO 基準稽核

- **日期**：2026-08-15
- **網址**：https://siokiu.criterium.tw/
- **專案**：`/vol2/ai/projects/TCU約騎系統`
- **範圍**：依 `/vol2/ai/projects/TCU SEO check/HANDOFF.md` 的 P2 —— schema 已達標不重做，
  重點放在 technical、content、on-page、Core Web Vitals、AI search readiness
- **性質**：基準稽核（本站第一次），所有數據為 2026-08-15 實測

---

## SEO 健康分數

| 類別 | 權重 | 分數 |
|---|---:|---:|
| Technical SEO | 22% | **72** |
| Content Quality | 23% | **18** |
| On-Page SEO | 20% | **38** |
| Schema / 結構化資料 | 10% | **64** |
| Performance (CWV) | 10% | **55** |
| AI Search Readiness | 10% | **74** |
| Images | 5% | **70** |

### 總分：50 / 100

**SPA 空殼**。有 H1 但只有 185 字元可見文字，H2/H3 全無。約騎路線這種天然長尾內容完全沒有進到 HTML。

> **Performance 評分限制**：本輪**無法取得實測 Core Web Vitals** ——
> 本機無 Chrome 執行檔（`chrome-devtools` MCP 回報 `Could not find Google Chrome executable`），
> PageSpeed Insights API 配額已用盡（`429 Quota exceeded`）。
> Performance 分數以可實測的代理指標推導：TTFB、HTML 位元組數、
> inline / 外部 JS 量、快取標頭、已知會拖累 CWV 的第三方腳本。
> **此分數不等同於 Google 實測值，應補跑一次 PSI 或 Lighthouse 確認。**

---

## 實測基線

| 項目 | 值 |
|---|---|
| HTTP 狀態 | 200 |
| TTFB | 0.452s |
| HTML 大小 | 298 KB |
| 可見文字 | 185 字元 |
| `<title>` | 42 字元 |
| `<meta description>` | 146 字元 |
| canonical | `https://siokiu.criterium.tw/` |
| `<html lang>` | `zh-Hant-TW` |
| H1 / H2 / H3 | 1 / 0 / 0 |
| 首頁內部連結 | 16 |
| 圖片（缺 alt） | 0（0） |
| 外部 JS / inline JS | 1 支 / 4 KB |
| JSON-LD 區塊 / `@type` 總數 | 2 / 16 |
| 懸空 `@id` | 無 |
| hreflang | x-default, zh-Hant, zh-TW |
| robots.txt / sitemap.xml / llms.txt | 200 / 200 / 200 |
| sitemap URL 數（帶 lastmod） | 7（7） |
| robots.txt 有 AI 爬蟲規則 | 是 |
| HSTS / CSP / X-Content-Type-Options | 有 / 有 / 有 |
| cf-cache-status | dynamic |
| Rocket Loader / Bot 挑戰腳本 | 無 / 無 |

**JSON-LD `@type`**：Answer, Country, FAQPage, ImageObject, Offer, Person, PostalAddress, Question, SportsOrganization, WebApplication

---

## 問題與建議

### 1. SPA 空殼：185 字元，H2 / H3 全無 `Critical`

**證據：** 可見文字 185 字元（title + description + noscript 提示 + 頁尾連結），H2 / H3 各 0 個，`<div id="root"></div>` 空。HTML 298 KB。

**建議：** 約騎平台的核心價值是「22 縣市經典路線」這類長尾內容 —— 目前這些完全沒有進到 HTML。至少把路線清單與縣市頁做成可索引的靜態內容。

### 2. 22 縣市路線頁沒有進 sitemap `High`

**證據：** sitemap 只有 7 個 URL，但 description 宣稱涵蓋「北中南東 22 縣市經典自行車路線」。這些路線頁若存在卻不在 sitemap，等於放棄整批長尾。

**建議：** 確認路線頁是否為獨立 URL；若是 SPA 路由則需先做可索引化，再進 sitemap。

### 3. Cloudflare 沒有快取 HTML `High`

**證據：** `cf-cache-status: dynamic`、`max-age=0, must-revalidate`。

**建議：** 加 Cache Rule。

### 4. sitemap-index 404 `Medium`

**證據：** `/sitemap-index.xml` 回 404（`/sitemap.xml` 正常）。

**建議：** 若不使用就從 robots.txt 移除引用。

---

## 七站共通問題：Cloudflare 沒有快取 HTML

七站中有六站回應 `cf-cache-status: dynamic`（coach 連這個標頭都沒有），
`cache-control` 多為 `public, max-age=0, must-revalidate` —— 等於明示不要快取。
TTFB 全部落在 **0.40–0.49s**，對靜態或半靜態站來說偏慢，而 TTFB 直接進 LCP。

| 站 | cf-cache-status | cache-control | TTFB |
|---|---|---|---|
| power | dynamic | `public, max-age=0, must-revalidate` | 0.40s |
| coach | （無標頭） | `private, no-cache, no-store, max-age=0` | 0.41s |
| miaoli | dynamic | `public, max-age=0, must-revalidate` | 0.45s |
| seh | dynamic | `public, max-age=0, must-revalidate` | 0.49s |
| gear | dynamic | `public, max-age=0, must-revalidate` | 0.47s |
| strava | dynamic | `public, max-age=300, s-maxage=300` | 0.42s |
| siokiu | dynamic | `public, max-age=0, must-revalidate` | 0.45s |

**24EZGO 也是同一個狀況**（2026-08-15 P1 稽核發現）。這是帳號層級的共通設定問題，
建議一次處理完八個站，而不是各站各自加 Cache Rule。
strava 的回應標頭其實已經寫對（有 `s-maxage`），但 `cf-cache-status` 仍是 dynamic，
代表 Cloudflare 端的 Cache Rule 才是關鍵，不是原站標頭。

---

## 家族實體一致性（跨站，非本站獨有）

本輪七站掃描把品牌家族的 `@id` 分岔全貌攤開了。同一個法人目前有 **六種** org `@id`：

| org `@id` | 使用的站 |
|---|---|
| `https://www.criterium.tw/#organization` | power、miaoli、seh、gear、strava、siokiu（多數派） |
| `https://criterium.tw/#organization`（無 www） | coach |
| `https://sehstudio.tw/#organization` | coach |
| `https://seh.com.tw/#organization` | seh、newtcu |
| `https://gear.criterium.tw/#organization` | power（**複製貼上遺留**） |
| `https://24ezgo.seh.com.tw/#organization` | 24ezgo |

author 實體有 **三種**：

| author `@id` | 使用的站 |
|---|---|
| `https://www.tsu.com.tw/#person` | coach、gear、strava、siokiu、24ezgo、newtcu（宿主） |
| `https://seh.com.tw/about#person` | seh |
| 無 Person 節點 | miaoli |
| Person 有節點但無 `@id` | power |

⚠️ **這需要一次跨站決策，不是各站各自修**：newtcu 於 2026-08-15 依當時證據改用
`https://seh.com.tw/#organization`，但本輪掃描顯示家族多數派（六站中的五站）用的是
`https://www.criterium.tw/#organization`。**要嘛 newtcu 回到 criterium，要嘛其餘五站改過來**，
在決定之前各站不要各自動手，否則只會多生一種寫法。

另外一條家族鐵則已於 2026-08-15 修正：`@id` **不跨頁解析**，非首頁頁面只寫
`{ "@id": OWNER_ID }` 等於懸空參照，author 訊號在該頁完全失效。
**每個掛 author 的頁面都要內聯完整 Person 節點**，跨站一致靠「`@id` 相同 + 欄位值相同」達成。
24EZGO 的 `src/data/jsonld.ts` 的 `authorPerson()` 是正確範例。

---

## 本次未能驗證的部分

- **Core Web Vitals 實測**：無 Chrome、PSI 配額 429。見上方 Performance 評分限制。
- **CrUX 欄位資料**：無。
- **Search Console**：未接，無索引涵蓋率與查詢資料。本報告的內容判斷基於頁面本體，
  不含實際搜尋表現。
- **深層頁面**：本輪只取首頁完整分析 + sitemap 全量清點，未逐頁抓取。
